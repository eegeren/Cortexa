{
  "name": "cortexa-railway",
  "version": "1.0.0",
  "description": "Cortexa Financial Intelligence Platform for Railway",
  "main": "main.py",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r build ../static",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "dev": "python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload"
  },
  "engines": {
    "node": "18.x",
    "python": "3.11.x"
  },
  "keywords": ["cryptocurrency", "trading", "ai", "financial"],
  "author": "Cortexa Team",
  "license": "MIT"
}